<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.*" />
    <PackageReference Include="xunit" Version="2.*" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.*" />
  </ItemGroup>

  <ItemGroup>
    <!-- These tests call SqlLibrary directly, so they need the program. -->
    <ProjectReference Include="..\ScanIngest.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- SqlLibrary reads queries.sql from beside the running binary, so the test
         run needs its own copy. Linked rather than duplicated: one file on disk,
         copied to both output folders at build time. -->
    <None Include="..\queries.sql" Link="queries.sql" CopyToOutputDirectory="PreserveNewest" />
    <!-- The parser tests read the real weekly exports, so they need copies
         beside the test binary (under samples/weekly/). -->
    <None Include="..\samples\weekly\*.nessus" Link="samples\weekly\%(Filename)%(Extension)" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
